import Redis, { RedisOptions } from 'ioredis';
export declare class RedisClient {
    client: Redis;
    subscriber: Redis;
    constructor(connectionOptions: RedisOptions | string);
    get(key: string): Promise<string | null>;
    set(key: string, value: string): Promise<'OK'>;
    close(): void;
}
