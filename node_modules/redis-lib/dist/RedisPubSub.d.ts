import { RedisClient } from './RedisClient';
export type MessageHandler = (channel: string, message: string) => void;
export declare class RedisPubSub {
    private redis;
    private handlers;
    constructor(redis: RedisClient);
    subscribe(room: string, handler: MessageHandler): void;
    publish(room: string, data: string): Promise<void>;
}
