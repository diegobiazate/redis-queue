import { RedisClient } from './RedisClient';
export declare class ClusterStrategy {
    private redis;
    private room;
    constructor(redis: RedisClient, room: string);
    publishToAll(data: string): Promise<void>;
    publishToOne(data: string): Promise<void>;
}
